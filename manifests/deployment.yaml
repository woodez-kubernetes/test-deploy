apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-test
  namespace: woodez-database
  labels:
    app: mongodb-testing
  annotations:
        checksum/git-commit: 55ac6759bc0020e1c58ca2a66f7436c3a732fc20
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-test
  template:
    metadata:
      labels:
        app: mongodb-test
    spec:
      containers:
      - name: mongodb-test-container
        image: kwood475/python-mongodb-test:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-test-service
  namespace: woodez-database 
  labels:
    app: mongodb-test
spec:
  ports:
  - port: 3000
    protocol: TCP
  selector:
    app: mongodb-test